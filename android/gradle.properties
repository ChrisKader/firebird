# Android Gradle plugin requires Java 11+
org.gradle.java.home=/Library/Java/JavaVirtualMachines/jdk-24.jdk/Contents/Home

# Android SDK configuration (required by build.gradle)
# These are normally set by QtCreator/androiddeployqt when building via Qt
androidCompileSdkVersion=33
androidBuildToolsVersion=34.0.0

# Qt Android platform path (required when building/syncing from IDE)
# Update this if your Qt installation is elsewhere
qt5AndroidDir=/Users/ck/Qt/5.15.2/android
